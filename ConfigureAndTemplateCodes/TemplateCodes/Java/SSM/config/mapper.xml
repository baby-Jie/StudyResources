<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myjava.dao.IUserDao">

    <!--配置映射-->
    <resultMap id="userMapper" type="com.myjava.domain.User">
        <id property="userId" column="id"></id>
        <result property="userName" column="name"></result>
        <result property="userBirthday" column="birthday"></result>
        <result property="userSex" column="sex"></result>
        <result property="userAddress" column="address"></result>
    </resultMap>
    <!--配置查询所有-->
    <select id="findAll" resultType="com.myjava.domain.User">
        select * from user;
    </select>

    <insert id="insertUser" parameterType="user">
        insert into user(username, sex, address) values( #{userName}, #{userSex}, #{userAddress})
    </insert>
</mapper>